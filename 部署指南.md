# 无人机资讯静态网站部署指南

本指南将帮助您将无人机资讯系统部署为可公开访问的静态网站。

---

## 📋 目录
1. [前置准备](#前置准备)
2. [本地测试](#本地测试)
3. [GitHub仓库设置](#github仓库设置)
4. [Vercel部署](#vercel部署)
5. [Netlify部署](#netlify部署)
6. [Cloudflare Pages部署](#cloudflare-pages部署)
7. [自动更新设置](#自动更新设置)

---

## 🎯 前置准备

### 1. 生成静态网站

首先在本地运行静态网站生成器：

```bash
# Windows
python static_site_generator.py

# 或者使用我们的部署脚本
python deploy_static_site.py
```

### 2. 准备GitHub账号
- 如果没有GitHub账号，先在 https://github.com 注册一个
- 完全免费

---

## 🧪 本地测试

### 步骤1：生成静态网站

```bash
cd e:\TRAE_PROJECT\UAV_info_collect
python static_site_generator.py
```

运行后会生成：
```
static_site/
├── index.html              # 首页
├── list.html               # 资讯列表（第1页）
├── list-2.html            # 资讯列表（第2页）
├── ...
├── articles/              # 文章详情
│   ├── 1.html
│   ├── 2.html
│   └── ...
└── assets/
    ├── css/style.css
    └── js/
```

### 步骤2：本地预览

生成完成后，可以直接用浏览器打开测试：

```bash
# 直接用浏览器打开
start static_site\index.html
```

或者使用Python启动一个本地服务器：

```bash
cd static_site
python -m http.server 8000
```

然后在浏览器访问：http://localhost:8000

---

## 📦 GitHub仓库设置

### 步骤1：创建GitHub仓库

1. 访问 https://github.com/new
2. 填写仓库信息：
   - 仓库名称：`drone-information`（或您喜欢的名字）
   - 描述：无人机前沿情报系统
   - 选择：Public（公开）或 Private（私有）
   - **不要**勾选 "Initialize this repository"
3. 点击 **Create repository**

### 步骤2：初始化Git仓库

在项目目录打开命令行：

```bash
# 如果还没有git仓库，先初始化
git init
```

### 步骤3：准备.gitignore文件

在项目根目录创建 `.gitignore` 文件：

```
# Python
__pycache__/
*.pyc
*.pyo

# 数据库
data/*.db

# IDE
.vscode/
.idea/

# 临时文件
*.tmp
*.log
```

### 步骤4：添加文件并提交

```bash
# 添加static_site目录到git
git add static_site/
git add static_site_generator.py
git add deploy_static_site.py
git add .gitignore

# 提交
git commit -m "初始化静态网站"

# 添加远程仓库（替换YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/drone-information.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

---

## 🚀 Vercel部署（推荐，最快最简单）

### 步骤1：访问Vercel

访问：https://vercel.com

### 步骤2：使用GitHub账号登录

1. 点击 **Sign Up** 或 **Continue with GitHub**
2. 授权Vercel访问您的GitHub账号

### 步骤3：导入项目

1. 点击 **Add New** → **Project**
2. 在仓库列表中选择您的 `drone-information` 仓库
3. 点击 **Import**

### 步骤4：配置项目

在配置页面设置：

- **Project Name**: drone-information（自动生成，可修改）
- **Framework Preset**: 选择 **Other**（或者留空）
- **Root Directory**: 设置为 `static_site/`
- **Build Command**: 留空（不需要构建）
- **Output Directory**: 留空（就是static_site）

### 步骤5：部署

点击 **Deploy** 按钮，等待1-2分钟！

### 步骤6：获取网站链接

部署完成后，Vercel会给您一个类似这样的链接：
- `https://drone-information.vercel.app`
- 或者您可以自定义域名

---

## 🚀 Netlify部署（简单易用）

### 步骤1：访问Netlify

访问：https://www.netlify.com

### 步骤2：注册或登录

使用GitHub账号登录

### 步骤3：导入项目

1. 点击 **Add new site** → **Deploy with GitHub**
2. 授权Netlify访问GitHub
3. 选择您的仓库

### 步骤4：配置

- **Publish directory**: `static_site`
- 其他留空

### 步骤5：部署

点击 **Deploy site**

### 步骤6：完成

部署后您会获得一个 `.netlify.app` 域名！

---

## 🚀 Cloudflare Pages部署（速度快，免费）

### 步骤1：访问Cloudflare Pages

访问：https://pages.cloudflare.com

### 步骤2：创建项目

1. 点击 **Create a project** → **Connect to Git**
2. 选择GitHub仓库

### 步骤3：配置

- **Build command**: 留空
- **Build output directory**: `static_site`
- 点击 **Save and Deploy**

---

## 🔄 自动更新设置

### 方案A：手动更新（简单）

每次有新资讯时：

```bash
# 1. 生成新的静态网站
python deploy_static_site.py

# 2. 推送到GitHub
git add static_site/
git commit -m "更新资讯"
git push
```

Vercel/Netlify/Cloudflare会自动检测到更新并重新部署！

### 方案B：GitHub Actions自动更新（推荐，自动化）

在项目中创建 `.github/workflows/update-site.yml`：

```yaml
name: 更新静态网站

on:
  # 每天凌晨2点自动运行
  schedule:
    - cron: '0 18 * * *'  # UTC时间，对应北京时间凌晨2点
  # 也可以手动触发
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: 设置Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          
      - name: 生成静态网站
        run: |
          # 这里需要处理数据库，或者使用本地数据
          # 如果需要远程数据库，需要配置
          
      - name: 提交并推送
        run: |
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add static_site/
          git commit -m "自动更新资讯" || echo "无变化"
          git push
```

### 方案C：使用本地定时任务（Windows）

创建一个批处理脚本 `update_and_deploy.bat`：

```batch
@echo off
echo 正在更新静态网站...

cd /d e:\TRAE_PROJECT\UAV_info_collect
python deploy_static_site.py

echo 正在推送到GitHub...
git add static_site/
git commit -m "自动更新资讯"
git push

echo 完成！
pause
```

然后使用Windows任务计划程序每天自动运行。

---

## 📱 手机访问

部署完成后，您可以：

1. **直接在手机浏览器打开**：访问您的Vercel/Netlify链接
2. **添加到主屏幕**：
   - iOS：Safari浏览器 → 分享 → 添加到主屏幕
   - Android：Chrome浏览器 → 菜单 → 添加到主屏幕
3. **分享给朋友**：直接发送链接即可！

---

## 🔗 常见问题

### Q: 我的网站是中文的，中文显示正常吗？
A: 是的！我们的模板已经设置了UTF-8编码，中文显示完全正常。

### Q: 可以自定义域名吗？
A: 可以！Vercel/Netlify/Cloudflare Pages都支持添加自定义域名。

### Q: 每天可以更新几次？
A: Vercel/Netlify免费版每天可以构建很多次，完全足够。

### Q: 需要付费吗？
A: 完全免费！所有这些平台都有非常慷慨的免费套餐。

### Q: 网站会有广告吗？
A: 不会！这些平台的免费套餐不会在您的网站上添加广告。

---

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看各平台的官方文档
2. 检查浏览器控制台的错误信息
3. 确认文件路径是否正确

---

## ✅ 部署检查清单

- [ ] 本地运行 `python static_site_generator.py` 成功
- [ ] 在浏览器测试 `static_site/index.html` 正常
- [ ] 创建了GitHub仓库并推送代码
- [ ] 在Vercel/Netlify/Cloudflare导入项目
- [ ] 设置正确的发布目录为 `static_site/`
- [ ] 部署成功获得访问链接
- [ ] 手机可以正常访问
- [ ] 设置了自动更新（可选）

---

## 🌐 平台对比

| 平台 | 速度 | 易用性 | 自定义域名 | 推荐度 |
|------|------|--------|-----------|--------|
| Vercel | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐⭐ |
| Netlify | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐ |
| Cloudflare Pages | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐ |

**个人推荐Vercel**，因为部署最快，体验最好！

---

祝您部署顺利！🚀
