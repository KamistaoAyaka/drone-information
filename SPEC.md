# 无人机前沿情报采集系统 - 项目规格说明

## 1. 项目概述

**项目名称**: 无人机前沿情报采集系统 (DroneScout)
**项目类型**: Python CLI工具 + Web可视化应用
**核心功能**: 自动采集、清洗、去重、分类和展示无人机行业前沿情报
**目标用户**: 无人机专业工程人员、研究人员和行业分析师

## 2. 功能范围

### 2.1 核心功能清单

- [x] **多源数据采集**
  - 支持从国内外10+个无人机资讯网站批量采集
  - 支持RSS订阅源采集
  - 支持关键词搜索采集
  - 自动识别文章标题、链接、日期、内容

- [x] **数据清洗处理**
  - 自动清洗HTML标签和脚本
  - 去除广告和无关内容
  - 规范化日期格式
  - 提取正文摘要

- [x] **智能去重**
  - 基于SimHash算法的文本相似度检测
  - URL精确去重
  - 标题相似度去重
  - 可配置相似度阈值

- [x] **自动分类**
  - 区域分类：国内/国外
  - 无人机类型：多旋翼/固定翼/垂直起降/无人直升机/仿生无人机
  - 应用领域：军事国防/民用消费/商业应用/科研教育
  - 公司分类：自动识别公司名称

- [x] **关键词提取**
  - 产品特色关键词提取
  - 技术路径关键词提取
  - 使用TF-IDF算法

- [x] **数据存储**
  - SQLite数据库存储
  - 文章表和公司表设计
  - 支持多条件查询

- [x] **命令行工具 (CLI)**
  - 数据采集命令
  - 数据搜索命令
  - 数据导出命令
  - 统计信息展示
  - 公司分类查看

- [x] **Web可视化界面**
  - 分类筛选（区域、类型、时间）
  - 关键词搜索
  - 文章卡片展示
  - 公司分类统计
  - 数据导出功能
  - 响应式设计

- [x] **示例数据**
  - 初始化5篇示例文章
  - 覆盖不同地区、公司、类型

### 2.2 用户交互流程

#### 快速启动流程
```
1. 用户安装依赖: pip install -r requirements.txt
2. 初始化示例数据: python drone_scout.py init
3. 启动Web服务: python drone_scout.py web --port 5000
4. 访问 http://localhost:5000 查看界面
```

#### 数据采集流程
```
1. 用户执行采集命令
2. 系统从配置的数据源批量采集
3. 数据清洗和去重
4. 自动分类和关键词提取
5. 存储到数据库
6. 返回采集结果统计
```

#### Web界面使用流程
```
1. 用户访问Web页面
2. 选择分类条件（区域、类型、时间）
3. 输入关键词（可选）
4. 点击"搜索"按钮
5. 系统查询并展示结果
6. 用户可查看详情或导出数据
```

## 3. 技术架构

### 3.1 系统架构图

```
┌─────────────────────────────────────────────────────┐
│                   用户交互层                         │
│  ┌─────────────┐    ┌──────────────────────────┐   │
│  │  CLI 工具    │    │    Web 可视化界面         │   │
│  │ drone_scout │    │    web/index.html         │   │
│  └─────────────┘    └──────────────────────────┘   │
└─────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────┐
│                   Flask API层                       │
│              api/routes.py (RESTful API)            │
└─────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────┐
│               数据处理层 (scraper/)                 │
│  ┌─────────────┐  ┌──────────┐  ┌───────────────┐   │
│  │ collector   │→ │ cleaner  │→ │ deduplicator  │   │
│  │ (采集器)     │  │ (清洗器)  │  │ (去重器)      │   │
│  └─────────────┘  └──────────┘  └───────────────┘   │
│                        ↓                            │
│              ┌──────────────────┐                    │
│              │   classifier    │                    │
│              │   (分类器)       │                    │
│              └──────────────────┘                    │
└─────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────┐
│               数据存储层 (database/)                 │
│              SQLite 数据库 + ORM                    │
│              articles表 + companies表              │
└─────────────────────────────────────────────────────┘
```

### 3.2 核心模块说明

**collector.py** - 数据采集器
- 支持网页爬虫、RSS订阅、API接口
- 实现增量采集和频率控制
- 自动解析日期格式

**cleaner.py** - 数据清洗器
- HTML标签和脚本清洗
- 噪声内容去除
- 文本规范化和截断

**deduplicator.py** - 数据去重器
- SimHash指纹生成
- 相似度计算
- 多级去重策略

**classifier.py** - 数据分类器
- 基于规则的分类
- 公司名称识别
- 关键词提取

**db.py** - 数据库操作
- SQLite封装
- CRUD操作
- 统计查询

**routes.py** - Flask API
- RESTful风格接口
- JSON数据交换
- 前端数据服务

### 3.3 数据库设计

```sql
-- 文章表
articles (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    content TEXT,
    url TEXT UNIQUE,
    source TEXT,
    publish_date TEXT,
    collected_date TEXT,
    region TEXT,
    drone_type TEXT,
    company TEXT,
    keywords TEXT,
    summary TEXT,
    features TEXT,
    method TEXT,
    simhash TEXT,
    is_processed INTEGER DEFAULT 0
)

-- 公司表
companies (
    id TEXT PRIMARY KEY,
    name TEXT UNIQUE NOT NULL,
    country TEXT,
    region TEXT,
    article_count INTEGER DEFAULT 0,
    latest_article_date TEXT
)
```

## 4. Web界面规格

### 4.1 页面结构

```
┌─────────────────────────────────────────────────┐
│                   页面标题                        │
│           "无人机前沿情报系统"                    │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐    │
│  │            统计数据栏                    │    │
│  │  总文章数 | 公司数 | 国内 | 国外          │    │
│  └─────────────────────────────────────────┘    │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐    │
│  │           筛选条件区域                    │    │
│  │  区域: [全部][国内][国外]                 │    │
│  │  类型: [全部][多旋翼][固定翼][垂直起降]   │    │
│  │  时间: [一周][一月][三月]                 │    │
│  │  搜索: [______________] [搜索] [导出]    │    │
│  └─────────────────────────────────────────┘    │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐    │
│  │  [文章列表] [公司分类]  (Tab切换)         │    │
│  └─────────────────────────────────────────┘    │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐    │
│  │           文章卡片网格                    │    │
│  │  ┌────────┐ ┌────────┐ ┌────────┐       │    │
│  │  │ 文章1  │ │ 文章2  │ │ 文章3  │       │    │
│  │  └────────┘ └────────┘ └────────┘       │    │
│  └─────────────────────────────────────────┘    │
└─────────────────────────────────────────────────┘
```

### 4.2 视觉设计

**配色方案**:
- 主色调: 深蓝色 (#0a192f)
- 次要色: 中蓝色 (#172a45)
- 强调色: 青绿色 (#64ffda)
- 背景色: 深色系 (#112240)
- 文字色: 浅色 (#e6f1ff)

**字体**:
- 主字体: 系统默认字体栈
- 标题: 粗体, 渐变色

**间距**:
- 容器最大宽度: 1400px
- 卡片间距: 25px
- 内边距: 20-30px

**动效**:
- 卡片悬停: 上浮5px + 阴影增强
- 按钮悬停: 渐变背景 + 下移2px
- 加载状态: 旋转动画
- 标签切换: 渐变显示

### 4.3 响应式断点

- 桌面端: > 768px (3列卡片)
- 平板端: 768px (2列卡片)
- 移动端: < 768px (1列卡片)

## 5. CLI工具规格

### 5.1 命令列表

| 命令 | 功能 | 示例 |
|------|------|------|
| init | 初始化示例数据 | `python drone_scout.py init` |
| collect | 采集数据 | `python drone_scout.py collect --all` |
| search | 搜索数据 | `python drone_scout.py search --region 国内` |
| export | 导出数据 | `python drone_scout.py export --format json` |
| stats | 统计信息 | `python drone_scout.py stats` |
| companies | 公司分类 | `python drone_scout.py companies` |
| web | 启动Web服务 | `python drone_scout.py web --port 5000` |

### 5.2 参数说明

**collect 命令参数**:
- `--all`: 采集所有数据源
- `--keyword`: 关键词搜索
- `--source`: 指定数据源

**search 命令参数**:
- `--region`: 区域筛选 (all/国内/国外)
- `--type`: 无人机类型筛选
- `--company`: 公司名称筛选
- `--keyword`: 关键词搜索
- `--days`: 时间范围(天)
- `--limit`: 结果数量限制

**export 命令参数**:
- `--format`: 导出格式 (json/csv/markdown)
- `--output`: 输出文件路径
- `--region`: 区域筛选
- `--type`: 类型筛选
- `--keyword`: 关键词
- `--days`: 时间范围

**web 命令参数**:
- `--host`: 监听地址 (默认0.0.0.0)
- `--port`: 监听端口 (默认5000)

## 6. API接口规格

### 6.1 接口列表

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | /api/articles | 获取文章列表 |
| GET | /api/statistics | 获取统计信息 |
| GET | /api/companies | 获取公司列表 |
| GET | /api/classify | 获取分类数据 |
| GET | /api/keywords | 获取关键词 |
| POST | /api/collect | 执行数据采集 |
| GET | /api/export | 导出数据 |

### 6.2 请求参数

**GET /api/articles**:
- region: 区域筛选
- type: 类型筛选
- company: 公司筛选
- keyword: 关键词
- days: 时间范围
- limit: 数量限制
- offset: 偏移量

### 6.3 响应格式

```json
{
  "success": true,
  "data": [...],
  "count": 100
}
```

## 7. 示例数据规格

### 7.1 示例文章结构

```json
{
  "id": "uuid-001",
  "title": "大疆发布全新Mavic 4无人机:搭载1英寸传感器和智能避障3.0",
  "content": "大疆创新今日正式发布Mavic 4无人机...",
  "url": "https://www.dji.com/news/mavic-4",
  "source": "大疆官网",
  "publish_date": "2024-01-15",
  "region": "国内",
  "drone_type": "多旋翼",
  "company": "大疆创新",
  "keywords": ["Mavic 4", "1英寸传感器", "智能避障"],
  "features": "1英寸传感器、智能避障3.0、45分钟续航",
  "method": "全新光学设计,搭载AI芯片"
}
```

### 7.2 预置示例

- 大疆Mavic 4新品发布 (国内/多旋翼)
- 极飞P100 Pro农业无人机 (国内/多旋翼)
- Wing配送无人机澳大利亚部署 (国外/垂直起降)
- DJI Mavic 3 Pro国际版 (国外/多旋翼)
- 亿航eVTOL型号合格证 (国内/垂直起降)

## 8. 性能要求

- 页面加载时间: < 2秒
- API响应时间: < 500ms
- 批量采集: 10-20篇文章/分钟
- 数据库查询: < 100ms
- 支持1000+文章数据

## 9. 兼容性要求

- Python版本: 3.8+
- 浏览器: Chrome, Firefox, Safari, Edge
- 操作系统: Windows, macOS, Linux
- 数据库: SQLite 3.x

## 10. 部署要求

### 10.1 环境准备

```bash
# Python环境
Python 3.8+

# 安装依赖
pip install -r requirements.txt
```

### 10.2 快速启动

```bash
# 初始化数据
python drone_scout.py init

# 启动Web服务
python drone_scout.py web --port 5000

# 访问
http://localhost:5000
```

### 10.3 目录结构

```
drone_scout/
├── drone_scout.py          # 主程序
├── requirements.txt        # 依赖
├── database/
│   └── db.py              # 数据库
├── scraper/
│   ├── collector.py       # 采集器
│   ├── cleaner.py          # 清洗器
│   ├── deduplicator.py    # 去重器
│   └── classifier.py     # 分类器
├── api/
│   └── routes.py          # API
├── web/
│   └── index.html         # 界面
├── config/
│   └── sources.py         # 配置
└── data/                  # 数据目录
    └── drone_scout.db     # 数据库文件
```

## 11. 验收标准

### 11.1 功能验收

- [x] CLI工具所有命令正常运行
- [x] Web界面正常显示和交互
- [x] 数据采集功能正常
- [x] 分类筛选功能正常
- [x] 数据导出功能正常
- [x] 示例数据正常显示

### 11.2 质量验收

- [x] 代码结构清晰，模块化设计
- [x] 错误处理完善
- [x] 文档齐全
- [x] 可直接运行

### 11.3 用户验收

- [ ] 用户能够快速启动系统
- [ ] 用户能够轻松理解和使用功能
- [ ] 界面美观流畅
- [ ] 数据准确可靠

---

**文档版本**: 1.0
**创建日期**: 2024-01-15
**最后更新**: 2024-01-15
