# 无人机前沿情报采集系统 - 整体架构设计

## 一、系统概述

本系统旨在为无人机专业工程人员提供自动化的前沿情报采集工具,能够从国内外主流无人机相关网站批量获取最近三个月的最新研究进展,并提供数据可视化和分类检索功能。

### 目标用户
- 无人机专业工程人员
- 无人机研究人员
- 无人机行业分析师

## 二、系统架构

```
┌─────────────────────────────────────────────────────┐
│                   用户交互层                         │
│  ┌─────────────┐    ┌──────────────────────────┐   │
│  │  CLI 工具    │    │    Web 可视化界面         │   │
│  │ (命令行)     │    │    (分类筛选+展示)         │   │
│  └─────────────┘    └──────────────────────────┘   │
└─────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────┐
│                   数据服务层                         │
│  ┌─────────────┐    ┌──────────────────────────┐   │
│  │  Flask API  │    │    数据检索服务            │   │
│  │  (后端服务)  │    │    (查询、排序、分页)      │   │
│  └─────────────┘    └──────────────────────────┘   │
└─────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────┐
│                   数据处理层                         │
│  ┌─────────────┐    ┌──────────────────────────┐   │
│  │ 数据清洗模块 │    │    数据去重模块           │   │
│  │ (清洗HTML)  │    │    (相似度去重)           │   │
│  └─────────────┘    └──────────────────────────┘   │
│  ┌─────────────┐    ┌──────────────────────────┐   │
│  │ 数据分类模块 │    │    关键词提取模块          │   │
│  │ (分类标签)  │    │    (关键词识别)            │   │
│  └─────────────┘    └──────────────────────────┘   │
└─────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────┐
│                   数据采集层                         │
│  ┌─────────────┐    ┌──────────────────────────┐   │
│  │ 网站爬虫模块 │    │    RSS 订阅模块            │   │
│  │ (多源采集)  │    │    (实时更新)              │   │
│  └─────────────┘    └──────────────────────────┘   │
│  ┌─────────────┐    ┌──────────────────────────┐   │
│  │ 数据源管理   │    │    调度器                 │   │
│  │ (配置管理)  │    │    (定时任务)              │   │
│  └─────────────┘    └──────────────────────────┘   │
└─────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────┐
│                   数据存储层                         │
│  ┌──────────────────────────────────────────────┐   │
│  │              SQLite 数据库                    │   │
│  │  - articles (文章表)                         │   │
│  │  - companies (公司表)                        │   │
│  │  - keywords (关键词表)                       │   │
│  │  - categories (分类表)                      │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

## 三、核心数据模型

### 3.1 文章数据模型 (Article)
```python
{
    "id": "UUID",
    "title": "文章标题",
    "content": "文章内容摘要",
    "url": "原始链接",
    "source": "来源网站",
    "publish_date": "发布日期",
    "collected_date": "采集日期",
    "region": "国内/国外",
    "type": "无人机类型(多旋翼/固定翼/垂直起降等)",
    "company": "所属公司",
    "keywords": ["关键词1", "关键词2"],
    "summary": "AI生成的摘要",
    "features": "产品特色/技术亮点",
    "method": "研制方法/技术路径",
    "is_processed": "是否已处理"
}
```

### 3.2 公司数据模型 (Company)
```python
{
    "id": "UUID",
    "name": "公司名称",
    "country": "国家",
    "region": "国内/国外",
    "description": "公司简介",
    "drone_types": ["主要产品类型"],
    "article_count": "相关文章数量",
    "latest_article_date": "最新文章日期"
}
```

## 四、数据采集策略

### 4.1 数据源列表

**国内数据源:**
- 新浪军事/无人机专栏
- 搜狐军事
- 网易军事
- 腾讯军事
- 人民网军事
- 凤凰网军事
- 无人机之家
- 无人机在线
- 极客公园(无人机板块)
- 36氪(无人机相关)

**国外数据源:**
- DroneLife
- sUAS News
- UAS Vision
- Aviation Today
- The Drone Girl
- UAV Coach
- DJI News
- Parrot News

### 4.2 采集方法

1. **网页爬虫采集**
   - 使用 requests + BeautifulSoup 进行网页抓取
   - 实现增量采集,避免重复
   - 设置合理的采集间隔(3-5秒)
   - 模拟浏览器请求头

2. **RSS订阅采集**
   - 支持RSS/Atom feeds
   - 实时性高,更新及时

3. **关键词搜索采集**
   - 支持按关键词搜索
   - 关键词: 无人机、UAV、drone、四旋翼、固定翼、大疆、DJI等

### 4.3 时间范围控制
- 默认采集最近3个月的数据
- 支持自定义时间范围
- 定时任务: 每天凌晨2点自动更新

## 五、数据处理流程

### 5.1 数据清洗流程
```
原始数据 → HTML解析 → 文本提取 → 噪声去除 → 数据规范化
```

清洗步骤:
1. 去除HTML标签和脚本
2. 去除广告和无关内容
3. 提取正文内容
4. 规范化日期格式
5. 去除特殊字符
6. 统一编码(UTF-8)

### 5.2 数据去重流程
```
数据入库前 → SimHash去重 → 相似度计算 → 去重判定 → 入库/丢弃
```

去重策略:
- 基于SimHash算法计算文本指纹
- 相似度阈值: 0.85
- 标题+内容组合去重
- URL精确去重

### 5.3 数据分类流程
```
清洗后数据 → 规则分类 → NLP增强 → 标签生成 → 分类完成
```

分类维度:
1. **地区分类**: 国内 / 国外
2. **无人机类型**:
   - 多旋翼无人机 (四旋翼、六旋翼、八旋翼)
   - 固定翼无人机
   - 垂直起降无人机 (VTOL)
   - 无人直升机
   - 仿生无人机
   - 其他
3. **应用领域**:
   - 军事国防
   - 民用消费
   - 商业应用
   - 科研教育
4. **公司分类**: 按公司名称分组

### 5.4 关键词提取
使用TF-IDF算法提取关键词:
- 产品特色关键词: 续航、载重、智能避障、自主飞行等
- 技术路径关键词: AI、机器视觉、5G通信、集群控制等
- 公司名称提取和归一化

## 六、数据可视化方案

### 6.1 命令行界面 (CLI)
```
功能特性:
- 交互式命令行界面
- 支持参数化查询
- 数据导出功能(JSON/CSV/Markdown)
- 实时进度显示
- 日志记录
```

CLI命令示例:
```bash
# 采集所有数据
drone_scout collect --all

# 按关键词采集
drone_scout collect --keyword "大疆 DJI 新型号"

# 按分类筛选
drone_scout search --region 国内 --type 四旋翼

# 按公司查询
drone_scout search --company "大疆创新"

# 数据导出
drone_scout export --format json --output data.json

# 启动Web服务
drone_scout web --port 5000

# 查看统计
drone_scout stats
```

### 6.2 网页可视化界面

**页面结构:**
```
┌─────────────────────────────────────────────────┐
│              无人机前沿情报系统                   │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐    │
│  │         分类筛选区域                      │    │
│  │  地区: [国内○/国外○/全部●]                │    │
│  │  类型: [多旋翼□/固定翼□/VTOL□/全部●]      │    │
│  │  时间: [最近一周○/一个月●/三个月○]        │    │
│  │  关键词: [___________________]          │    │
│  │             [开始采集]                   │    │
│  └─────────────────────────────────────────┘    │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐    │
│  │         数据展示区域                      │    │
│  │  ┌───────────────────────────────────┐   │    │
│  │  │ 文章卡片 1                         │   │    │
│  │  │ 标题: xxx                         │   │    │
│  │  │ 来源: xxx  公司: xxx  日期: xxx   │   │    │
│  │  │ 摘要: xxxxxxxxxxxxxxxxx          │   │    │
│  │  └───────────────────────────────────┘   │    │
│  │  ┌───────────────────────────────────┐   │    │
│  │  │ 文章卡片 2                         │   │    │
│  │  └───────────────────────────────────┘   │    │
│  └─────────────────────────────────────────┘    │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐    │
│  │         公司研究分类区域                  │    │
│  │  ┌─────────────────────────────────┐    │    │
│  │  │ 大疆创新 (15篇)                  │    │    │
│  │  │ ████████████░░░░ 75%           │    │    │
│  │  │ 最新文章: 2024-01-15            │    │    │
│  │  └─────────────────────────────────┘    │    │
│  └─────────────────────────────────────────┘    │
└─────────────────────────────────────────────────┘
```

**页面功能:**
1. **分类筛选**: 支持多维度组合筛选
2. **数据卡片**: 展示文章标题、来源、公司、日期、摘要
3. **公司分类**: 按公司展示研究情况统计
4. **关键词云**: 展示当前筛选结果的高频关键词
5. **时间轴**: 按时间线展示数据更新
6. **导出功能**: 支持JSON/CSV/Markdown格式导出

**技术实现:**
- 前端: HTML5 + CSS3 + JavaScript (无框架依赖)
- 后端: Flask (Python)
- 响应式设计,支持桌面和移动端
- 实时搜索和筛选

## 七、技术选型

### 7.1 后端技术栈
- **语言**: Python 3.8+
- **Web框架**: Flask
- **数据库**: SQLite (轻量级,无需配置)
- **HTTP请求**: requests
- **HTML解析**: BeautifulSoup4
- **文本处理**: jieba (中文分词)
- **数据去重**: SimHash
- **定时任务**: APScheduler

### 7.2 前端技术栈
- **HTML5**: 语义化标签
- **CSS3**: Flexbox布局, CSS变量
- **JavaScript**: 原生ES6+, Fetch API
- **无框架依赖**: 纯前端实现,降低复杂度

### 7.3 项目结构
```
drone_scout/
├── drone_scout.py          # CLI主程序
├── scraper/
│   ├── __init__.py
│   ├── collector.py       # 数据采集器
│   ├── cleaner.py          # 数据清洗
│   ├── deduplicator.py     # 数据去重
│   ├── classifier.py      # 数据分类
│   └── keywords.py         # 关键词提取
├── database/
│   ├── __init__.py
│   ├── models.py           # 数据模型
│   ├── db.py               # 数据库操作
│   └── queries.py          # 查询接口
├── api/
│   ├── __init__.py
│   └── routes.py           # API路由
├── web/
│   ├── index.html          # 主页面
│   ├── css/
│   │   └── style.css       # 样式文件
│   └── js/
│       └── app.js          # 前端逻辑
├── config/
│   └── sources.py          # 数据源配置
├── utils/
│   ├── __init__.py
│   ├── logger.py           # 日志工具
│   └── date_utils.py       # 日期工具
├── data/
│   └── drone_scout.db      # SQLite数据库
├── requirements.txt        # 依赖列表
├── SPEC.md                 # 规格说明
└── README.md               # 使用说明
```

## 八、数据示例

### 8.1 示例数据
```json
{
  "articles": [
    {
      "id": "uuid-001",
      "title": "大疆发布全新Mavic 4无人机:搭载1英寸传感器和智能避障3.0",
      "content": "大疆创新今日正式发布Mavic 4无人机,该机型采用全新设计的1英寸CMOS传感器...",
      "url": "https://www.dji.com/news/mavic-4",
      "source": "大疆官网",
      "publish_date": "2024-01-15",
      "region": "国内",
      "type": "多旋翼",
      "company": "大疆创新",
      "keywords": ["Mavic 4", "1英寸传感器", "智能避障", "4K视频"],
      "features": "1英寸传感器、智能避障3.0、45分钟续航",
      "method": "全新光学设计,搭载AI芯片",
      "is_processed": true
    }
  ]
}
```

### 8.2 分类结果示例
```json
{
  "国内多旋翼": [
    {"company": "大疆创新", "count": 15, "latest": "2024-01-15"},
    {"company": "极飞科技", "count": 8, "latest": "2024-01-14"},
    {"company": "零度智控", "count": 5, "latest": "2024-01-13"}
  ],
  "国外固定翼": [
    {"company": "Wing", "count": 6, "latest": "2024-01-14"},
    {"company": "Amazon Prime Air", "count": 4, "latest": "2024-01-12"}
  ]
}
```

## 九、运行要求

### 9.1 环境要求
- Python 3.8+
- 网络连接(用于数据采集)
- 磁盘空间: 至少100MB

### 9.2 依赖安装
```bash
pip install -r requirements.txt
```

### 9.3 快速启动
```bash
# 命令行模式 - 采集所有数据
python drone_scout.py collect --all

# 命令行模式 - 按关键词搜索
python drone_scout.py search --keyword "大疆" --region 国内

# 启动Web服务
python drone_scout.py web --port 5000

# 查看帮助
python drone_scout.py --help
```

## 十、注意事项

1. **遵守网站规则**: 爬虫应遵守robots.txt和网站使用条款
2. **频率控制**: 避免高频请求,设置适当的采集间隔
3. **数据时效性**: 定期更新数据,保持信息的时效性
4. **错误处理**: 完善的异常捕获和日志记录
5. **数据备份**: 定期备份数据库文件
